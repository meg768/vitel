module.exports = {
	darkMode: 'class',
	theme: {
		// Some useful comment
		fontFamily: {
			nunito: ['lato', 'sans-serif'],
			MyFont: ['"My Font"', 'serif'] // Ensure fonts with spaces have " " surrounding it.
		}
	},
	variants: {
		// Some useful comment
	},
	plugins: [
		// Some useful comment
	]
};
