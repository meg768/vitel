# Vitel — Claude-kontext

## Canonical Source
- All project information is maintained in `CONTEXT.md`.
- All project memory and future updates must be saved in `CONTEXT.md`.
- `CLAUDE.md` is a pointer file only and should not contain duplicated project memory.
